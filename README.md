# maze
A Streamlit-based maze app where users create mazes by setting walls, paths, start, and end points. It solves the maze using DFS and BFS, visualizes the paths, and compares the number of explored states for both algorithms
